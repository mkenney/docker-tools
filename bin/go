#!/usr/bin/env sh


if [ "self-update" = "$1" ]; then
    docker pull golang:1.7-alpine
else
    if [ -t 0 ]; then
        term_type=ti
    else
        term_type=i
    fi
    docker run --rm -$term_type -v $(pwd):/src:rw -v $HOME/.ssh:/home/dev/.ssh:ro mkenney/go:latest $@
fi
